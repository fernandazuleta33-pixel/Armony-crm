name: CI/CD Armony CRM

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Validación básica
        run: echo "Proyecto listo para deploy"

      - name: Listar archivos (debug)
        run: ls -la